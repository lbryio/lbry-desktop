import LANGUAGES from './languages';

const SUPPORTED_LANGUAGES = {
  en: LANGUAGES.en[1],
  pl: LANGUAGES.pl[1],
  id: LANGUAGES.id[1],
  de: LANGUAGES.de[1],
};

export default SUPPORTED_LANGUAGES;
